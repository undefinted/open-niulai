# 模型连接中心

Open NiuLai 当前采用 RunningHub 统一执行层。用户连接一个自己的 RunningHub API Key，再选择 MiniMax H3、Seedance 或自定义视频工作流；所选工作流消耗该 RunningHub 账户对应的额度。

## 当前支持

| 能力 | 当前方式 | 说明 |
| --- | --- | --- |
| MiniMax H3 | RunningHub 工作流预设 | 用户绑定自己的工作流 ID 与提示词节点 |
| Seedance | RunningHub 工作流预设 | 用户绑定自己的多参考或视频工作流 |
| 其他开源模型 | RunningHub 自定义工作流 | 使用用户有权调用的工作流及节点映射 |
| 本地 SVD | 可验证样片 | 只证明仓库内一次真实推理，不冒充任意输入的即时结果 |
| MiniMax 官方 API | 后端兼容适配器 | 不在默认网页中展示，避免两套账户体验并存 |

## OAuth 扩展边界

BYOK 不是 OAuth。只有供应商公开第三方应用注册、授权端点、令牌端点、scope 和撤销机制，并批准 Open NiuLai 客户端后，才能启用正式 OAuth。项目不会复用其他应用的 client ID，也不会用网页登录 Cookie 模拟授权。

## 安全规则

- 公网环境未同时配置 HTTPS、签名会话、任务存储和限流时，拒绝上传素材和提交或查询视频任务。
- API Key 只保存在当前标签页，不写文件、数据库、响应、任务历史或 Git 仓库。
- 工作流访问密码和上传素材不进入 Open NiuLai 持久化存储。
- 每个付费任务必须再次确认，并带防重复提交标识；任务创建发生不确定错误时不自动重试。
- 浏览器保存最近 20 条非敏感任务摘要，生产端匿名任务元数据最多保留七天。
